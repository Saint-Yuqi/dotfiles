# Path to your Oh My Zsh installation.
export ZSH="$HOME/.oh-my-zsh"

#ZSH_THEME="agnoster"
ZSH_THEME=""

# Disable compfix warning
ZSH_DISABLE_COMPFIX=true

plugins=(
     git
     zsh-autosuggestions
     zsh-syntax-highlighting
     z
)

HIST_STAMPS="mm/dd/yyyy"
# Source Oh My Zsh
source $ZSH/oh-my-zsh.sh

#Brew
eval "$(/opt/homebrew/bin/brew shellenv)"
source /opt/homebrew/Cellar/zsh-syntax-highlighting/0.8.0/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh

# >>> conda initialize >>>
# !! Contents within this block are managed by 'conda init' !!
__conda_setup="$('/Users/yang/anaconda3/bin/conda' 'shell.zsh' 'hook' 2> /dev/null)"
if [ $? -eq 0 ]; then
    eval "$__conda_setup"
else
    if [ -f "/Users/yang/anaconda3/etc/profile.d/conda.sh" ]; then
        . "/Users/yang/anaconda3/etc/profile.d/conda.sh"
    else
        export PATH="/Users/yang/anaconda3/bin:$PATH"
    fi
fi
unset __conda_setup
# <<< conda initialize <<<
# export PATH="/opt/homebrew/Caskroom/anaconda/2024.10-1/anaconda3/bin:$PATH"  # commented out by conda initialize

export PATH="$PATH:/Applications/Visual Studio Code.app/Contents/Resources/app/bin"
#export PYTHONPATH="/Users/yang/Panels/optim/GarmentCode-main/pygarment:$PYTHONPATH"
export PYTHONPATH="/Users/yang/Panels/optim/GarmentCode-main:$PYTHONPATH"

export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion

# Added by Antigravity
export PATH="/Users/yang/.antigravity/antigravity/bin:$PATH"

. "$HOME/.local/bin/env"
export PATH="$HOME/.local/bin:$PATH"

eval "$(starship init zsh)"
alias ssh="TERM=xterm-256color ssh"
function y() {
    local tmp="$(mktemp -t "yazi-cwd.XXXXXX")"
    yazi "$@" --cwd-file="$tmp"
    if cwd="$(cat -- "$tmp")" && [ -n "$cwd" ] && [ "$cwd" != "$PWD" ]; then
        builtin cd -- "$cwd"
    fi
    rm -f -- "$tmp"
}
